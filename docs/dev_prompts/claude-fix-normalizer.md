## 目的
- 既存のbackend/rag/normalizer.py内のdef _split_heading_linebreaks　の記述を処理を、backend/rag/logic/chunking/chunking_logic_03.py　に移行したいです。
- 今後、複数種類のファイルを取り込みするにあたって、構造文書特有の正規化を、構造文書用のchukingロジックに切り離したいという意図です。

## 要件
- ingestのフローを行った際に、共通のnormalizerはファイルの中身に対してある程度共通して使えるものにしてください。例えば、日記や図式入りのテキストなども想定しています。
    - _spilit_heading_linebreaks以外の箇所でも、変更や移行が推奨されるものは教えてください。

- chunking_logic_03.py側では、既存のコードや定数を流用できるところは統合してコード量を削減してください。
- chunkign_logic_03.py側の正規化について、子チャンクの内側の列挙に関してデータクレンジングがきれいに見える改行削除をしたいです。
    - OCRがやや不安定なため、ヘッダーパターンのある列挙が、連番かつ、間に空行を除き改行を挟む別要素がない限り、二重改行を削除して単一改行になるようにしてください。

- KISS原則に則り、必要十分な実装を行ってください。